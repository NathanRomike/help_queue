import { colorLocation } from 'help-queue/helpers/color-location';
import { module, test } from 'qunit';

module('Unit | Helper | color location');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = colorLocation([42]);
  assert.ok(result);
});
