import { timeColor } from 'help-queue/helpers/time-color';
import { module, test } from 'qunit';

module('Unit | Helper | time color');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = timeColor([42]);
  assert.ok(result);
});
